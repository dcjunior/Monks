using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClickableTile : MonoBehaviour {

	public float tileX;
	public TileMap map;

	void Start(){
		//hero = Hero.GetComponent<HeroController> ();
		//villain = Villain.GetComponent<EnemyController> ();
	}

	void FixedUpdate(){
		//if (Input.GetMouseButtonDown (0)) {
			//Debug.Log ("Click! TileX= " + tileX + " TilePos =  " + hero.tilePos); 
			//hero.check = true;
			//map.check = true;
			//map.move (tileX);
			//map.tilePos = tileX;
			//hero.tilePos = tileX;
			//hero.move(tileX);
		//}
		//OnMouseUp ();
	}
		
}
